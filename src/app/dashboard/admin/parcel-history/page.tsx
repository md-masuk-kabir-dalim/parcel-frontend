import ParcelHistory from '@/components/dashboard/admin/parcel-history';
import React from 'react';

const ParcelHistoryPage = () => {
    return <ParcelHistory />;
};

export default ParcelHistoryPage;
